---
---
<h1>
  Pipal Academy
</h1>

<dl>
  {% for collection in site.collections %}
    <dt>
      {{ collection.title }}
    </dt>
    <dd>
      {% if collection.output %}
        <ul>
          {% for doc in collection.docs %}
            <li>
              <a href="{{ './' | append: doc.url }}">
                {{ doc.title }}
              </a>
            </li>
          {% endfor %}
        </ul>
      {% else %}
        <dl>
          {% for doc in collection.docs %}
            <dt>{{ doc.title }}</dt>
            <dd>{{ doc.content }}</dd>
          {% endfor %}
        </dl>
      {% endif %}
    </dd>
  {% endfor %}
</dl>
